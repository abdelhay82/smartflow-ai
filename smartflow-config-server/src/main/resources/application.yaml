spring:
  application:
    name: smartflow-config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/abdelhay82/smartflow-config-repo.git
          default-label: main
          clone-on-start: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/




server:
  port: 8888
